#include <bits/stdc++.h>

using namespace std;
const int INF = 1e9;
subt(set[],subset,size,sunsetsize,total,nodecou,sum){
    if(sum==totls){
        print(subset);
        subt(set,subset,size,subsetsize-1,total-set[nodecount],nodecount+1,)
    }
    else{
        
    }
}
int main()
{
    sibt(set[],subset[],size,0,0,0,sum)
    
}
